html, body,
:global(#root )
  width 100%
  height 100%
:global(.app) 
  width 100%
  height 100%
  transform rotate(0)
  display flex
  background url("~@@/common/1.jpeg")
.app-test
  color red
.footer 
  transform translate(0,0)